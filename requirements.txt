# Selenium is used for web automation and testing
selenium==4.18.1 
# Webdriver-manager is used to manage selenium webdriver
webdriver-manager==4.0.1
# Psycopg2-binary is a PostgreSQL database adapter for Python
psycopg2-binary==2.9.9
# Python-dotenv is used to load environment variables
python-dotenv==1.0.1
# BeautifulSoup4 is a library for parsing HTML and XML documents
beautifulsoup4==4.12.3
# Requests is a library for making HTTP requests
requests==2.31.0
# Pandas is a library for data manipulation and analysis
pandas==2.2.1
# Undetected-chromedriver is a library for using ChromeDriver without being detected
undetected-chromedriver==3.5.5 